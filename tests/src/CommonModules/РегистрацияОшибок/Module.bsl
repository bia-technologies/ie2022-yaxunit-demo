
&Вместо("ЗафиксироватьОшибку")
Процедура ЮТЗафиксироватьОшибку(Ошибка) Экспорт
	ПараметрыМетода = Мокито.МассивПараметров(Ошибка);
	
	ПрерватьВыполнение = Ложь;
	Мокито.АнализВызова(РегистрацияОшибок, "ЗафиксироватьОшибку", ПараметрыМетода, ПрерватьВыполнение);
	
	Если НЕ ПрерватьВыполнение Тогда
		ПродолжитьВызов(Ошибка);
	КонецЕсли;
КонецПроцедуры
